---
name: result-sheet-templates-under-release
description: Move Result Sheet Templates page under Release page as nested child
status: in_progress
date: 2026-04-15
quick_id: 260415-c7o
---

## Tasks

### task-1: Update routes for nested result-sheet-templates

**Files:**
- `routes/web.php`

**Action:**
- Move result-sheet-template routes from `/admin` prefix to be nested under `/release` prefix (child route of release)
- Create new route group: `Route::prefix('release')->name('release.')` containing result-sheet-templates
- Old route: `admin.result-sheet-templates.index` → New route: `release.result-sheet-templates.index`
- Keep the preview route: `/release/result-sheet-templates/preview`

**Verification:**
- Run `php artisan route:list | grep result-sheet` to confirm new route structure

### task-2: Update sidebar navigation

**Files:**
- `resources/js/Layouts/AuthenticatedLayout.svelte`

**Action:**
- Find the sidebar navigation definition
- Move Result Sheet Templates from standalone item to become child of Release section
- Convert Release to have nested items array with Result Sheet Templates as child

**Verification:**
- Check sidebar in browser shows Release with nested Result Sheet Templates child item

### task-3: Update breadcrumbs in Result Sheet Templates pages

**Files:**
- `resources/js/Pages/Admin/ResultSheetTemplates/Index.svelte`
- `resources/js/Pages/Admin/ResultSheetTemplates/Create.svelte`
- `resources/js/Pages/Admin/ResultSheetTemplates/Edit.svelte`

**Action:**
- Update each page's breadcrumbs to include Release as parent:
  ```javascript
  const breadcrumbs = [
    { label: 'Release Management', href: '/release' },
    { label: 'Result Sheet Templates' }
  ];
  ```

**Verification:**
- Navigate to result-sheet-templates and verify breadcrumb shows Release > Result Sheet Templates

### task-4: Verify all links updated

**Action:**
- Search codebase for any hard links to `/admin/result-sheet-templates` and update to `/release/result-sheet-templates`
- Check controller redirect responses and Blade links

**Verification:**
- Test navigation: Release page → click Result Sheet Templates → should work