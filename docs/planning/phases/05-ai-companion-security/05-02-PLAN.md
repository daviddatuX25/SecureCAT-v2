---
phase: 05-ai-companion-security
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - app/Services/AiCompanionService.php
autonomous: true
requirements:
  - Security hardening for AI Companion feature
user_setup: []

must_haves:
  truths:
    - "Code generation requests are detected and blocked"
    - "Prompt injection attempts are detected and blocked"
    - "Unsafe content is detected and blocked"
    - "Input is sanitized (strip_tags) before processing"
  artifacts:
    - path: "app/Services/AiCompanionService.php"
      provides: "Security guardrails in service layer"
      contains: "containsCodeGenerationRequest|containsPromptInjection|containsUnsafeContent"
  key_links:
    - from: "AiCompanionService::chat()"
      to: "Guardrail methods"
      via: "Checks before API call"
      pattern: "containsCodeGenerationRequest.*return.*blocked"
---

<objective>
Implement security guardrails in AiCompanionService to block code generation, prompt injection, and unsafe content.

Purpose: Prevent AI from being used to generate harmful code, manipulate system prompts, or process unsafe content.
Output: Protected service with guardrail methods.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ai-companion-security/05-RESEARCH.md
@app/Services/AiCompanionService.php

<interfaces>
From app/Services/AiCompanionService.php:
```php
class AiCompanionService
{
    public const DEFAULT_MAX_HISTORY = 20;
    public function chat(Applicant $applicant, string $userMessage, int $maxHistory = self::DEFAULT_MAX_HISTORY): array;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add security guardrail methods</name>
  <files>app/Services/AiCompanionService.php</files>
  <read_first>app/Services/AiCompanionService.php</read_first>
  <action>
    1. Add constant WARNING_THRESHOLD_LENGTH = 1800
    2. Add constant WARNING_THRESHOLD_HISTORY = 17
    3. Add private method containsCodeGenerationRequest(string $message): bool with patterns:
       - '/write\s+(a\s+)?(php|javascript|python|sql|html|css)\s+(code|function|script)/i'
       - '/generate\s+(a\s+)?(php|javascript|python|sql|html|css)\s+(code|function|script)/i'
       - '/create\s+(a\s+)?(program|script|exploit|payload)/i'
       - '/how\s+to\s+(hack|exploit|bypass|inject)/i'
    4. Add private method containsPromptInjection(string $message): bool with patterns:
       - '/ignore\s+(previous|all|above)\s+(instructions?|rules?|prompts?)/i'
       - '/system\s*:/i'
       - '/<\|.*\|>/'
       - '/\[SYSTEM\]/i'
       - '/\#\#\#\s*SYSTEM/i'
       - '/override\s+(your\s+)?(instructions?|rules?)/i'
    5. Add private method containsUnsafeContent(string $message): bool with patterns:
       - '/violence/i'
       - '/threat/i'
       - '/self[-\s]?harm/i'
    6. Add private method sanitizeInput(string $message): string using strip_tags()
  </action>
  <verify>
    <automated>grep -n "containsCodeGenerationRequest\|containsPromptInjection\|containsUnsafeContent" app/Services/AiCompanionService.php</automated>
  </verify>
  <done>Security guardrail methods added to service</done>
  <acceptance_criteria>
    - Service has containsCodeGenerationRequest() method with 4+ patterns
    - Service has containsPromptInjection() method with 6+ patterns
    - Service has containsUnsafeContent() method with 3+ patterns
    - Service has sanitizeInput() method using strip_tags()
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Integrate guardrails into chat() method</name>
  <files>app/Services/AiCompanionService.php</files>
  <read_first>app/Services/AiCompanionService.php</read_first>
  <action>
    1. At the start of chat() method, before creating user message:
       a. Sanitize input: $userMessage = $this->sanitizeInput($userMessage);
    2. After sanitization, add guardrail checks:
       a. if ($this->containsCodeGenerationRequest($userMessage)) { return ['reply' => 'I\'m sorry, but I cannot help with code generation. I can assist with questions about our courses, admission requirements, and application process.', 'blocked' => true]; }
       b. if ($this->containsPromptInjection($userMessage)) { return ['reply' => 'I noticed an unusual request. Let\'s focus on helping with your application questions.', 'blocked' => true]; }
       c. if ($this->containsUnsafeContent($userMessage)) { return ['reply' => 'I\'m here to help with admission-related questions. Please rephrase your question.', 'blocked' => true]; }
    3. Update return type to include 'blocked' key in response array
    4. Ensure controller checks for 'blocked' key and handles appropriately
  </action>
  <verify>
    <automated>grep -n "blocked.*true" app/Services/AiCompanionService.php</automated>
  </verify>
  <done>Guardrails integrated into chat workflow</done>
  <acceptance_criteria>
    - chat() method calls sanitizeInput() before processing
    - chat() method returns 'blocked' => true for rejected requests
    - All three guardrail checks executed in order: sanitize -> code -> injection -> unsafe
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Update controller to handle blocked responses</name>
  <files>app/Http/Controllers/Portal/AiCompanionController.php</files>
  <read_first>app/Http/Controllers/Portal/AiCompanionController.php</read_first>
  <action>
    1. In chat() method, after getting $result from service:
    2. Check if isset($result['blocked']) && $result['blocked'] === true
    3. If blocked, still return 200 with the reply (user-friendly message)
    4. Include any warning logic from Plan 01
  </action>
  <verify>
    <automated>grep -n "blocked" app/Http/Controllers/Portal/AiCompanionController.php</automated>
  </verify>
  <done>Controller handles blocked responses</done>
  <acceptance_criteria>
    - Controller checks for 'blocked' key in service response
    - Blocked responses return 200 (user-friendly message, not error)
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applicant → AI Service | Untrusted user message crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-03 | Tampering | containsCodeGenerationRequest | mitigate | Block requests matching code generation patterns |
| T-05-04 | Tampering | containsPromptInjection | mitigate | Block requests attempting prompt manipulation |
| T-05-05 | Information Disclosure | containsUnsafeContent | mitigate | Block requests with unsafe content patterns |
| T-05-06 | Tampering | sanitizeInput | mitigate | strip_tags() removes HTML/script tags |
</threat_model>

<verification>
- Guardrail methods exist in service
- chat() method integrates all checks
- Controller handles blocked responses
</verification>

<success_criteria>
- Code generation requests blocked with user-friendly message
- Prompt injection attempts blocked with user-friendly message
- Unsafe content blocked with user-friendly message
- Input sanitized before processing
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-companion-security/{phase}-02-SUMMARY.md`
</output>
