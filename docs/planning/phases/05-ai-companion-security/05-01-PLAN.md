---
phase: 05-ai-companion-security
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/Providers/AppServiceProvider.php
  - routes/web.php
  - app/Http/Controllers/Portal/AiCompanionController.php
autonomous: true
requirements:
  - Security hardening for AI Companion feature
user_setup: []

must_haves:
  truths:
    - "Rate limiting is enforced per applicant (10 req/min)"
    - "Users receive friendly error when rate limited"
    - "Message length warnings appear at 1800+ chars"
    - "History limit warnings appear at 17+ messages"
  artifacts:
    - path: "app/Providers/AppServiceProvider.php"
      provides: "Rate limiter configuration for ai-companion"
      contains: "RateLimiter::for.*ai-companion"
    - path: "routes/web.php"
      provides: "Rate-limited chat routes"
      contains: "throttle:ai-companion"
    - path: "app/Http/Controllers/Portal/AiCompanionController.php"
      provides: "Warning messages in response"
      contains: "warning.*length|warning.*history"
  key_links:
    - from: "app/Providers/AppServiceProvider.php"
      to: "routes/web.php"
      via: "RateLimiter config + throttle middleware"
      pattern: "RateLimiter::for.*ai-companion.*throttle:ai-companion"
    - from: "AiCompanionController"
      to: "Service"
      via: "chat method returns warnings"
      pattern: "chat.*return.*warning"
---

<objective>
Implement per-user rate limiting and input validation warnings for AI Companion chat.

Purpose: Prevent abuse through rate limiting, improve UX with proactive warnings before limits are hit.
Output: Rate-limited chat endpoint with warning responses.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/05-ai-companion-security/05-RESEARCH.md
@app/Services/AiCompanionService.php
@app/Http/Controllers/Portal/AiCompanionController.php

<interfaces>
<!-- Key interfaces the executor needs. Extracted from codebase. -->

From app/Services/AiCompanionService.php:
```php
class AiCompanionService
{
    public const DEFAULT_MAX_HISTORY = 20;
    public function chat(Applicant $applicant, string $userMessage, int $maxHistory = self::DEFAULT_MAX_HISTORY): array;
    public function clearHistory(Applicant $applicant): void;
}
```

From app/Http/Controllers/Portal/AiCompanionController.php:
```php
public function chat(AiCompanionChatRequest $request): JsonResponse
{
    $result = $this->companionService->chat($applicant, $message);
    return response()->json(['reply' => $result['reply']]);
}
```

From routes/web.php:
```php
Route::post('ai-companion/chat', [AiCompanionController::class, 'chat'])->name('ai-companion.chat');
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Configure per-user rate limiter</name>
  <files>app/Providers/AppServiceProvider.php</files>
  <read_first>app/Providers/AppServiceProvider.php</read_first>
  <action>
    1. Add RateLimiter configuration in the boot() method after existing rate limiters
    2. Configure 'ai-companion' limiter: 10 requests per minute per applicant, fallback to IP for unauthenticated
    3. Configure 'ai-companion-clear' limiter: 5 requests per minute
    4. Use custom response with JSON message: ['message' => 'Too many requests. Please wait a moment.', 'retry_after' => 60]
  </action>
  <verify>
    <automated>grep -n "RateLimiter::for.*ai-companion" app/Providers/AppServiceProvider.php</automated>
  </verify>
  <done>Rate limiter configured with per-user throttling</done>
  <acceptance_criteria>
    - AppServiceProvider.php contains "RateLimiter::for('ai-companion'" with Limit::perMinute(10)
    - Rate limiter uses applicant ID as key: "ai-companion:" . $applicant->id
    - Fallback uses IP when applicant is null
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Apply throttle middleware to routes</name>
  <files>routes/web.php</files>
  <read_first>routes/web.php</read_first>
  <action>
    1. Update the portal ai-companion/chat route to use 'throttle:ai-companion' middleware
    2. Update ai-companion/clear-history route to use 'throttle:ai-companion-clear' middleware
  </action>
  <verify>
    <automated>grep -n "throttle:ai-companion" routes/web.php</automated>
  </verify>
  <done>Routes protected by rate limiting middleware</done>
  <acceptance_criteria>
    - Route POST ai-companion/chat includes "throttle:ai-companion" middleware
    - Route POST ai-companion/clear-history includes "throttle:ai-companion-clear" middleware
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add warning responses to controller</name>
  <files>app/Http/Controllers/Portal/AiCompanionController.php</files>
  <read_first>app/Http/Controllers/Portal/AiCompanionController.php</read_first>
  <action>
    1. In the chat() method, after getting the message, calculate message length
    2. Add WARNING_THRESHOLD_LENGTH = 1800 constant to AiCompanionService
    3. Add WARNING_THRESHOLD_HISTORY = 17 constant to AiCompanionService
    4. In controller: if message length > 1800, set $warning['length'] = 'You are approaching the 2000 character limit.'
    5. In controller: get history count, if >= 17, set $warning['history'] = 'You are approaching the message history limit. Consider clearing history.'
    6. Return warning in response: response()->json(['reply' => $result['reply'], 'warning' => $warning])
  </action>
  <verify>
    <automated>grep -n "warning.*length\|warning.*history" app/Http/Controllers/Portal/AiCompanionController.php</automated>
  </verify>
  <done>Controller returns warning messages when thresholds approached</done>
  <acceptance_criteria>
    - AiCompanionService has WARNING_THRESHOLD_LENGTH = 1800 constant
    - AiCompanionService has WARNING_THRESHOLD_HISTORY = 17 constant
    - Controller returns 'warning' key in JSON response
    - Warning includes length message when message > 1800 chars
    - Warning includes history message when history >= 17 messages
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| applicant → API | Untrusted input crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Denial of Service | ai-companion/chat endpoint | mitigate | Per-user rate limiting (10 req/min) prevents abuse |
| T-05-02 | Information Disclosure | Rate limit response | accept | Generic error message, no sensitive data exposed |
</threat_model>

<verification>
- Rate limiter configured in AppServiceProvider
- Routes use throttle middleware
- Controller returns warning in JSON response when thresholds exceeded
</verification>

<success_criteria>
- Rate limited endpoints return 429 with JSON message when limit exceeded
- Chat responses include warning key when approaching limits
- Per-user rate limiting enforced (applicant ID as key)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ai-companion-security/{phase}-01-SUMMARY.md`
</output>
